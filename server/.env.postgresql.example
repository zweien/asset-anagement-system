# PostgreSQL 数据库配置示例
# ============================
# 复制此文件为 .env 并修改配置
#
# 使用方法:
#   1. 复制文件: cp .env.postgresql.example .env
#   2. 修改数据库连接信息
#   3. 使用 PostgreSQL schema: cp prisma/schema.postgresql.prisma prisma/schema.prisma
#   4. 运行迁移: npx prisma generate && npx prisma db push

# 服务端口
PORT=3002

# 数据库连接 (PostgreSQL)
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/assets"

# JWT 密钥 (生产环境必须修改!)
JWT_SECRET=please-change-this-secret-in-production

# 上传目录
UPLOAD_DIR=uploads

# 最大文件大小 (bytes, 默认 10MB)
MAX_FILE_SIZE=10485760

# AI 配置 (可选，也可在前端设置页面配置)
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
AI_MODEL=deepseek-chat
AI_MAX_TOKENS=4096

# Node 环境
NODE_ENV=development
